<%= link_to page, url, { remote: remote, rel: page.next? ? "next" : page.prev? ? "prev" : nil, class: "w-8 h-8 flex items-center justify-center rounded text-gray-600 hover:text-blue-500 hover:bg-blue-50 text-xs font-medium" } %>
